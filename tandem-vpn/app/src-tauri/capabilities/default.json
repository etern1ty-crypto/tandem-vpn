{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window.",
  "windows": ["main", "tandem-vpn*"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
