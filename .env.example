# Optional process environment only. No .env file is loaded automatically.
# No passwords, API tokens or secrets are needed.
# Hide audit events on CLI stderr; protected on-disk audit logging stays enabled.
TANDEM_QUIET=0
# Browser preview only. Keep 5173 when using cargo tauri dev.
TANDEM_PREVIEW_PORT=5173
