{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Local main window only. No shell, filesystem or remote-origin plugin capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
